import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel handles serverless automatically — no output: export needed
  // This keeps full Next.js features while being serverless on Vercel
};

export default nextConfig;
